# Sheaf Axiom: Gluing (reconstruct global from local) (n=100, depth=10)
# Hypothesis: Gluing produces SHD=0 reconstruction

method: holograph
learning_rate: 0.01
lambda_descent: 1.0
lambda_spec: 0.1
lambda_reg: 0.0001
max_steps: 500
use_natural_gradient: true
llm_provider: sglang
llm_model_role: primary
use_llm: true
use_embeddings: false
use_active_queries: true
max_queries_per_step: 3
query_interval: 25
max_total_queries: 100
max_total_tokens: 500000
output_dir: experiments/outputs
save_checkpoints: true
checkpoint_interval: 100
experiment_id: X4_n100_d10
description: 'Sheaf Axiom: Gluing (reconstruct global from local) (n=100, depth=10)'
hypothesis: Gluing produces SHD=0 reconstruction
claims_supported:
- T4
dataset: sf
n_vars: 100
dataset_kwargs:
  avg_degree: 2.0
seed: 42
context_depth: 10
